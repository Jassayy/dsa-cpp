#include <iostream>

using namespace std;

int main()
{
      // int *ptr;
      // or
      int *ptr = NULL;
      cout << "Null pointer: " << ptr; // garbage value
      // we cant dereference the null pointer
      return 0;
}